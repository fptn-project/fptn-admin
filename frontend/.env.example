# Optional — defaults to a relative /api/v1, proxied to the backend by
# vite dev server (see vite.config.ts) or nginx in production. Only needed
# to point at a different backend.
# VITE_API_BASE_URL=http://localhost:8000/api/v1
